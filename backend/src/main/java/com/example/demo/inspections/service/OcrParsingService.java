package com.example.demo.inspections.service;

import org.springframework.stereotype.Service;

@Service
public class OcrParsingService {

    public String parseWithAI(String rawText) {

        // Placeholder for OpenAI call later
        // For now, just return the raw text

        return rawText;
    }
}